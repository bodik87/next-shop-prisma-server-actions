generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

model Item {
  id         Int    @id @default(autoincrement())
  title      String
  price      Float
  oldPrice   Float
  categoryId Int
}
